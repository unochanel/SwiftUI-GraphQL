query Repository($first: Int!, $query: String!) {
  search(first: $first, query: $query, type: Repositpry) {
      repositoryCount
      nodes {
        ... on Repository {
          id
          name
        }
      }
  }
}
